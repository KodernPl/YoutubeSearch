import React from 'react';
import styled from 'styled-components';

const SpinnerWrapper = styled.div`
  margin: 2rem 0;
`;

const Spinner = () => (<SpinnerWrapper>Loading...</SpinnerWrapper>);

export default Spinner;
